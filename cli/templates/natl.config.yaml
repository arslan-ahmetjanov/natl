engine: playwright
timeout: 15000
# Live sandbox on GitHub Pages (needs network).
base_url: https://natl-dev.github.io/natl/sandbox.html
# Offline twin (clone the repo, then uncomment and point at the file):
# base_url: path/to/natl/examples/fixtures/sandbox.html
headless: true
artifacts_dir: artifacts
trace: on-fail
video: off
