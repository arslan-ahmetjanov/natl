name: Release fixture login
engine: playwright
tags: [release, smoke]
vars:
  base_url: ./app.html
steps:
  - goto: $base_url
  - fill: "#email"
    with: demo@natl.dev
  - click: "#login"
  - assert: ".ok"
    visible: true
  - log: "get_user flow ok"
