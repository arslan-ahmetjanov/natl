name: Shop fixture smoke (Cypress)
engine: cypress
timeout: 60000
tags: [smoke, cypress]

vars:
  base_url: ./fixtures/shop.html

steps:
  - goto: $base_url
  - assert: "#login h1"
    text: "Login"
  - log: "shop fixture reachable via cypress"
