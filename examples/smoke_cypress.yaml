name: Sandbox fixture smoke (Cypress)
engine: cypress
timeout: 60000
tags: [smoke, cypress]

vars:
  base_url: ./fixtures/sandbox.html

steps:
  - goto: $base_url
  - assert: "#login h1"
    text: "Login"
  - log: "sandbox fixture reachable via cypress"
