# Copy to .env and fill in. Use in YAML as $env.TEST_USER / $secret.TEST_PASS
# (compat: ${ENV:TEST_USER}).
# Matches sandbox contract: password must be "secret"; welcome uses email local-part.
TEST_USER=demo@natl.dev
TEST_PASS=secret
