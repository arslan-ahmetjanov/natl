# Project defaults for examples.
# Walk-up from any scenario in this folder finds this file.
# Env overlays: config/staging.yaml, config/prod.yaml

engine: playwright
timeout: 15000
# Local twin of the docs sandbox (offline). Live Pages: use --env prod
base_url: ./fixtures/sandbox.html
headless: true
artifacts_dir: artifacts
trace: on-fail
video: off
